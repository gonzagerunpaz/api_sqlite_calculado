const ahora = new Date(); //=
const fecha = new Date("2000-04-21"); //=

const milisegundo = 1000 * 60 * 60 * 24 * 365.25; //=
const dif = Math.floor((ahora - fecha) / milisegundo); //=

const username = "gerardo";
username.charAt(0); //=
username.charAt(username.length - 1); //=
